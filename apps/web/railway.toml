[build]
buildCommand = "SKIP_ENV_VALIDATION=true pnpm install --frozen-lockfile && SKIP_ENV_VALIDATION=true pnpm turbo build --filter=web..."

[deploy]
startCommand = "cd apps/web && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
